@model EventDeleteViewModel
@inject Microsoft.AspNetCore.Antiforgery.IAntiforgery Antiforgery

@{
    ViewData["Title"] = $"Удалить — {Model.Event.TitlePage}";
    var token = Antiforgery.GetAndStoreTokens(Context).RequestToken!;
}

<div id="app"></div>

<script>
    window.__DATA__ = @Html.Raw(System.Text.Json.JsonSerializer.Serialize(new
    {
        @event = new
        {
            id = Model.Event.Id,
            titlePage = Model.Event.TitlePage,
            slug = Model.Event.Slug,
            transfer = Model.Event.Transfer != null ? new { name = Model.Event.Transfer.Name } : null,
            isPublished = Model.Event.IsPublished,
        },
        antiForgeryToken = token,
    }));
</script>

@section Scripts 
{
    <script type="module" vite-src="~/scripts/views/events-delete/page.tsx"></script>
}
